# first-nannou-project

This is a simple project to get started with Nannou.

Captures each frame and saves to the `capture` directory.

Set to render just one frame.

## How to run

```bash
cargo run --release
```
